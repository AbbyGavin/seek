# Personal preferences (not shared with team)

## My style
- Explain your reasoning briefly before showing code changes
- When writing tests, show both the happy path and one edge case, use the current test suite as a guide for style and structure
- Keep responses concise — I'm reading in a terminal alongside RubyMine
- Prefer early returns in methods
- Run `bundle exec rubocop` on the files I've changed. Fix any offences in the changed files only (don't touch unrelated files). dont write any rubocop related comments in the code, just fix the offences.

## My common tasks
- currently I mostly work on contentblob, assets 
- is managed by RVM (Ruby Version Manager) version 1.29.12.
  • Ruby version: 3.1.4p223 (arm64-darwin24)
  • Ruby path: ~/.rvm/rubies/ruby-3.1.4/bin/ruby


 